guard "jekyll-plus", drafts: true, future: true, config: ['_config.yml'] do
  # prevent other files from copying, such as the assets files.
  watch /.*\.[md|mkd|mkdn|markdown|textile|html|haml|slim|xml|yml]/
  ignore /^_site/
end